Flask
Gunicorn
keras
tensorflow
numpy
Werkzeug
matplotlib
scikit-learn
flask-cors
waitress
ultralytics
# Note: install PyTorch separately following official instructions at https://pytorch.org/
# Example (Windows CPU-only):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# For GPU install follow the selector on the PyTorch site for your CUDA version.